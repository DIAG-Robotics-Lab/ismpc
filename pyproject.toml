[project]
name = "ismpc"
version = "0.1.0"
description = "IS-MPC: a framework for humanoid gait generation"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "dartpy>=6.16",
    "casadi>=3.6",
    "scipy>=1.11",
    "matplotlib>=3.8",
    "numpy>=1.26",
    "osqp>=0.6",
    "pin>=2.7",
]

[tool.uv]
package = false
